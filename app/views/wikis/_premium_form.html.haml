%h1.center-text
  Private Wikis
  = link_to "New Wiki", new_wiki_path, class: "btn btn-success"
.row
  %table.center
    - @wikis.each do |wiki|
      - if wiki.private == true && current_user.id == wiki.user_id
        %tr
          %td
            = link_to wiki.title, wiki_path(wiki)
          %td
            = link_to "Edit", edit_wiki_path(wiki), class: "btn btn-success"
          %td
            = link_to "Delete Wiki", [wiki], method: :delete, class: "btn btn-danger", data: {confirm: 'Are you sure you want to delete #{wiki.title}'}
